server:
  port: 8083

spring:
  application:
    name: ms-tc-reservation

  datasource:
    url: jdbc:postgresql://localhost:5432/reservation
    username: youness
    password: admin

  cloud:
    discovery:
      enabled: false
    config:
      enabled: false

  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect
    database: postgresql